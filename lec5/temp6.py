card = (9, 'hearts')
card = (card[0],) + (8,) + (card[-1],)
print(card[0])

empty_tuple = ()
empty_tuple2 = tuple()

w = (2, 1,2)

q = (3,4,5,123)

print(w < q)

a, b = 1, 2
a, b = (1, 2)

cards = [(2,'set'), (3,'set'), (4,'set')]